{
  "name": "@spacetrash/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "SpaceTrash desktop app — scan, classify, confirm-to-act",
  "author": "SpaceTrash",
  "type": "module",
  "main": "./src/main.js",
  "scripts": {
    "bundle": "node scripts/bundle.mjs",
    "start": "npm run bundle && electron src/main.js",
    "dist": "npm run bundle && electron-builder --win nsis portable"
  },
  "dependencies": {
    "better-sqlite3": "^12.11.1"
  },
  "devDependencies": {
    "electron": "34.5.8",
    "electron-builder": "^26.0.12",
    "esbuild": "^0.25.9"
  }
}
